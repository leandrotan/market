The access token and refresh tokens are saved in Social_Media\youTube\bin so to get the right authentications u need to either
1- run the script from bin
2- copy the tokens into the directory from where you want to run the code

The source retrieves the created channels and uploaded videos on youtube the maximum retrieved resultset per call is 50 resource